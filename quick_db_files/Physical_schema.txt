# Physical
movies
- 
movie_id SERIAL PK 
country_id VARCHAR(40) 
company_id VARCHAR(40) 
genre_id VARCHAR(40) FK >- genre.genre_id
director_id VARCHAR(40) 
actor_id VARCHAR(40)
writer_id VARCHAR(40) FK >- writer.writer_id
rating_id VARCHAR(20) FK >- rating.rating_id
name VARCHAR(40)
release_date DATE
domestic_revenue_usd BIGINT
international_revenue_usd BIGINT
total_revenue_usd BIGINT 

writer
- 
writer_id SERIAL PK
writer  VARCHAR(255) 
writer_rank INT

director
- 
director_id SERIAL PK 
director VARCHAR(40)
director_rank INT

director_movie
- 
director_id BIGINT FK >- director.director_id
movie_id BIGINT FK >- movies.movie_id

company_movie
- 
company_id BIGINT FK >- company.company_id
movie_id BIGINT FK >- movies.movie_id

company
-
company_id SERIAL PK
company VARCHAR(255) 
company_rank INT

actor
- 
actor_id SERIAL PK
actor VARCHAR(255) 
actor_rank INT

actor_movie
- 
actor_id INT FK >- actor.actor_id
movie_id BIGINT FK >- movies.movie_id


genre
- 
genre_id SERIAL PK 
genre VARCHAR(255) 

rating
- 
rating_id SERIAL PK 
rating BIGINT 

# DBD limit on number of table to 10
# Therefore, comment out country, 
# country
#- 
#country_id SERIAL PK
#country VARCHAR(255) 












